services:
  alertmanager:
    build:
      context: .
    container_name: alertmanager
    restart: unless-stopped
    command:
      - "--config.file=/etc/alertmanager/alertmanager.yml"
    networks:
      - transcendence_nw